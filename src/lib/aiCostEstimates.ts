// Rough per-use cost estimates for the optional AI features, shown next to
// each one so self-hosters can gauge their Anthropic API spend before using
// them. These are approximations (actual cost depends on image size and
// response length) — not billing guarantees.
export const SCREENSHOT_COST_ESTIMATE = '$0.02–$0.03';
export const TEXT_PARSE_COST_ESTIMATE = '$0.01–$0.02';
export const SETTLE_SUGGEST_COST_ESTIMATE = '$0.03–$0.06';
export const AI_SUMMARY_COST_ESTIMATE = 'under $0.01';
